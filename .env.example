# CoreWeaver local secrets example. Copy values into your shell environment or
# an ignored .env file; never commit real keys.

GEMINI_API_KEY=
COREWEAVER_MODEL_ENDPOINT=https://generativelanguage.googleapis.com/v1beta/openai
COREWEAVER_MODEL=gemini-flash-latest
COREWEAVER_RUN_PROFILE=mock_swarm
